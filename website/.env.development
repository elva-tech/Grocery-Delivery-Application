# Local development — npm run dev
# Backend default port from backend/src/server.js is 5000.

VITE_API_BASE_URL=http://localhost:5000
VITE_API_URL=http://localhost:5000
VITE_TENANT_ID=mahalakshmi-computers

# TEMP: bypass "Store Closed" overlay for local API testing — remove when done
# VITE_BYPASS_STORE_CLOSED=true
