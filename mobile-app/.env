# Local dev — eNandi customer build (same as eas build --profile enandi-production)
CUSTOMER=sales

# Point to your PC backend — must match your current LAN/hotspot IP (run `ipconfig` if requests fail).
EXPO_PUBLIC_API_DEV_URL=http://172.20.10.3:5000