# Local development (loaded by Vite when you run: npm run dev)
# Override in .env.development.local (gitignored via *.local) for your machine only.

VITE_API_URL=http://localhost:5000
VITE_TENANT_ID=demo-tenant
